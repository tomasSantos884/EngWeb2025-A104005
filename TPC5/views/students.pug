

block content
    .w3-card-4
        header.w3-container.w3-teal
            h1 
                span Students List
                a.w3-btn.w3-round.w3-grey(href="/front/registo") +
        
        .w3-container
            if slist && slist.length > 0
                table.w3-table-all
                    tr
                        th Id
                        th Name
                        th GitLink
                        th Actions
                    each aluno in slist 
                        tr
                            td= aluno._id
                            td
                                a(href="/front/" + aluno._id)= aluno.nome
                            td= aluno.git
                            td
                                span [
                                a(href="/front/edit/" + aluno._id) Edit
                                span ]
                                span [ 
                                a(href="/front/delete/" + aluno._id) Delete
                                span ]
            else
                p Nenhum aluno encontrado.